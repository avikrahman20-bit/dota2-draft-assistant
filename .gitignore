# Secrets
.env
credentials.json
token.json

# Stratz cache: the single bundle + small JSONs are committed so Railway skips the fetch;
# the per-hero working dir is unpacked from the bundle at startup.
.tmp/matchups_stratz/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Database
users.db

# OS
.DS_Store
Thumbs.db
backups/
